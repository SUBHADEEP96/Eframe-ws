"use client";
import { useState } from "react"; import { contactSchema } from "@/lib/contact";
type Errors=Record<string,string[]>;
export function ContactForm(){const [errors,setErrors]=useState<Errors>({});const [status,setStatus]=useState("");const [pending,setPending]=useState(false);
 async function submit(event:React.FormEvent<HTMLFormElement>){event.preventDefault();setStatus("");const form=new FormData(event.currentTarget);const value={name:form.get("name"),email:form.get("email"),phone:form.get("phone")||undefined,organisation:form.get("organisation")||undefined,enquiry:form.get("enquiry"),message:form.get("message"),consent:form.get("consent")==="on",website:form.get("website")};const result=contactSchema.safeParse(value);if(!result.success){setErrors(result.error.flatten().fieldErrors as Errors);return}setErrors({});setPending(true);try{const response=await fetch("/api/contact",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(result.data)});const payload=await response.json();setStatus(payload.message);if(response.ok)event.currentTarget.reset()}catch{setStatus("We could not connect. Please email info@eframe.in directly.")}finally{setPending(false)}}
 const field=(name:string)=>errors[name]?.[0];return <form onSubmit={submit} className="contact-form" noValidate><div className="form-grid"><label>Name <span>*</span><input name="name" autoComplete="name" aria-invalid={!!field("name")} required/>{field("name")&&<small>{field("name")}</small>}</label><label>Email <span>*</span><input name="email" type="email" autoComplete="email" aria-invalid={!!field("email")} required/>{field("email")&&<small>{field("email")}</small>}</label><label>Phone<input name="phone" type="tel" autoComplete="tel"/></label><label>Organisation<input name="organisation" autoComplete="organization"/></label></div><label>Enquiry <span>*</span><select name="enquiry" defaultValue="" required><option value="" disabled>Select a service</option><option>Learning solutions</option><option>Immersive experiences</option><option>Creative studio</option><option>Enterprise solutions</option><option>General enquiry</option></select></label><label>Message <span>*</span><textarea name="message" rows={6} required aria-invalid={!!field("message")}/>{field("message")&&<small>{field("message")}</small>}</label><label className="consent"><input type="checkbox" name="consent"/> <span>I consent to Eframe using these details to respond to my enquiry. *</span></label>{field("consent")&&<small>{field("consent")}</small>}<label className="honeypot" aria-hidden="true">Website<input name="website" tabIndex={-1} autoComplete="off"/></label><button className="cta-button" disabled={pending}>{pending?"Sending…":"Send enquiry"}</button><p role="status" aria-live="polite">{status}</p></form>}
